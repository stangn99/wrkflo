import React from 'react';

const Header = () => {
  return (
    <div className="headerWrapper">
     <h4>I'm not sure if we need a header anymore with the Material-UI drawer in place. </h4>
    </div>
  );
}

export default Header;